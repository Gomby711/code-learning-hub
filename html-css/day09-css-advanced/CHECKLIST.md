# Day 9 Checklist

- [ ] The list items alternate background colors (odd vs even)
- [ ] Hovering the link changes its color
- [ ] Clicking into the input changes its border color
- [ ] The quote paragraph shows a curly opening quote before it and a curly closing quote after it, neither of which exists in your actual HTML text
- [ ] Hovering the "animated-button" smoothly fades to a darker color over about a third of a second, not an instant snap
- [ ] The ball continuously bounces up and down, forever, without you clicking anything
- [ ] The themed button's color and padding come from your `:root` custom properties — change `--primary-color` in DevTools live and confirm the button updates
- [ ] You can explain, in your own words, the difference between a pseudo-class (`:hover`) and a pseudo-element (`::before`)

Compare against `solution.css` if anything is unclear.
