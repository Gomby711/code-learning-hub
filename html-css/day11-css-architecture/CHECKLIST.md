# Day 11 Checklist

- [ ] Every class in your HTML follows the `block`, `block__element`, or `block__element--modifier` pattern
- [ ] The card still renders correctly (image, title, description, two differently-colored buttons) after renaming
- [ ] You can explain, in your own words, why `.card__title` is safer in a large project than a generic `.title`
- [ ] You can explain what a CSS reset does and why it's needed, given that browsers already apply their own default styles
- [ ] You can describe, at a high level, how you'd split a large project's CSS into multiple files (reset, variables, base, layout, components)

Compare against `solution.css`/`solution.html` if anything is unclear.
