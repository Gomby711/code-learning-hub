# Day 8 Checklist

- [ ] Your `<head>` includes the viewport meta tag
- [ ] Open DevTools' device toolbar (Ctrl+Shift+M) and set the width to something narrow (e.g. 375px, a phone size) — confirm the 3 boxes stack VERTICALLY
- [ ] Widen the device toolbar to 900px+ — confirm the boxes switch to sitting side by side HORIZONTALLY
- [ ] Confirm the heading is visibly smaller on the narrow/phone width and visibly larger on the wide/desktop width
- [ ] Drag the device toolbar width slowly through 768px and confirm the layout changes exactly at that breakpoint, not before or after
- [ ] You can explain, in your own words, the difference between `max-width` and `min-width` media queries, and which one this exercise used (and why, given it's mobile-first)

Compare against `solution.css` if anything is unclear.
