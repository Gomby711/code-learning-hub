# Day 6 Checklist

- [ ] The gallery shows 3 equal-width columns
- [ ] The "Featured" item visibly spans the first two columns, wider than the other items
- [ ] The page layout shows a header spanning the full width on top, a 200px sidebar and flexible main area side by side in the middle, and a footer spanning the full width on the bottom
- [ ] Resize your browser window narrower and confirm the sidebar stays a fixed 200px while the main area shrinks/grows to fill the rest
- [ ] In DevTools, most browsers show a small grid badge next to elements using `display: grid` in the Elements panel — click it to visualize the grid lines overlaid on the actual page
- [ ] You can explain, in your own words, why you'd choose Grid for the page layout in Part 2 but Flexbox would have been the better/simpler choice for something like a single row of navbar links

Compare against `solution.css` if anything is unclear.
